from flask_sqlalchemy import SQLAlchemy

# This file initializes the SQLAlchemy object which allows for interaction with the database.

# An instance of the SQLAlchemy class that represents the database.
orm = SQLAlchemy()
